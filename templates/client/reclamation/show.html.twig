{% extends 'client/index.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1 style="margin-top: 290px">Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>Typerec</th>
                <td>{{ reclamation.typerec }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reclamation.description }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ reclamation.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reclamation_index') }}">back to list</a>

    <a href="{{ path('reclamation_edit', {'id': reclamation.id}) }}">edit</a>

    {{ include('client/reclamation/_delete_form.html.twig') }}
{% endblock %}
